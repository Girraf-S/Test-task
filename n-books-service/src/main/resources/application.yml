spring:
  application:
    name: n-book-service
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

  datasource:
    url: jdbc:postgresql://localhost:5432/n-book-db
    username: postgres
    password: mitrandir

jwt:
  key: Nikon1234
  bearer: 'Bearer '
  begin-index: 7

server:
  port: 8082

service:
  user-domain: http://localhost:8081
  domain: http://localhost:8082



